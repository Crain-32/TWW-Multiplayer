package crain.model.enums;

/**
 * Used in the Admin Controller to give the Admin an Easier way to trigger Global Events.
 */
public enum EventTypes {
    MULTI,
    COOP,
    EVENT,
    NAME,
    GENERAL,
    ERROR
}
